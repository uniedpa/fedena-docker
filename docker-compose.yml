db:
  image: mysql
  environment:
  - MYSQL_ROOT_PASSWORD=root
  ports:
    - "3333:3306"
web:
  image: .
  command: bundle exec rails s -p 3000 -b '0.0.0.0'
  volumes:
    - .:/myapp
  ports:
    - "3000:3000"
  links:
    - db